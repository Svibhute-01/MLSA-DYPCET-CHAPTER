<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Tailwind (CDN) isolated: prefix + no preflight -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      prefix: 'tw-',
      corePlugins: { preflight: false },
      theme: {
        extend: {
          fontFamily: {
            mono: ['JetBrains Mono', 'ui-monospace', 'SFMono-Regular', 'Menlo', 'monospace'],
            orbitron: ['Orbitron', 'JetBrains Mono', 'ui-monospace', 'monospace'],
          },
          colors: {
            bg: '#0a0f1c',
            brand: '#00ff88',
            accent: '#00e5ff',
            purple: '#a855f7'
          }
        }
      }
    }
  </script>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&family=Orbitron:wght@500;700;800&display=swap" rel="stylesheet" />

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>

  <title>Add Team Member - MLSA DYPCET</title>
  <link rel="icon" href="assets/mlsa.jpeg">
  
  <style>
    :root {
      --muted: #94a3b8;
    }
    
    body {
      background-color: #0a0f1c;
      color: white;
      overflow-x: hidden;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
      background-image: 
        linear-gradient(rgba(10, 15, 28, 0.9) 1px, transparent 1px),
        linear-gradient(90deg, rgba(10, 15, 28, 0.9) 1px, transparent 1px);
      background-size: 50px 50px;
    }
    
    .tw-bg-gradient {
      background: linear-gradient(135deg, rgba(0,229,255,0.10) 0%, rgba(168,85,247,0.09) 100%);
    }
    
    .form-label {
      font-weight: 600;
      color: #0de5ff;
      letter-spacing: 0.03em;
      font-family: 'Orbitron', 'JetBrains Mono', monospace;
      font-size: 1.05rem;
      margin-bottom: 0.5rem;
    }
    
    .tw-header {
      font-family: 'Orbitron', 'JetBrains Mono', sans-serif;
      font-weight: 800;
      letter-spacing: .08em;
      color: #0de5ff;
      text-shadow: 0 0 24px #0de5ffa1;
      font-size: 1.8rem;
    }
    
    .btn-brand {
      background-color: #0de5ff;
      color: #101727;
      font-family: 'Orbitron', monospace;
      font-weight: 700;
      letter-spacing: .08em;
      border-radius: 0.6rem;
      box-shadow: 0 0 12px 0 rgba(13,229,255,0.12);
      border: none;
      transition: all 0.18s cubic-bezier(.45,.85,.5,1.6);
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
    }
    
    .btn-brand:hover {
      background-color: #a855f7;
      color: #fff;
      box-shadow: 0 0 48px #a855f7a9;
      transform: translateY(-2px);
    }
    
    .form-control {
      background: rgba(0, 0, 0, 0.3);
      border: 2px solid #a855f7;
      border-radius: 8px;
      color: white;
      padding: 0.75rem 1rem;
      transition: all 0.3s;
      font-size: 0.95rem;
    }
    
    .form-control:focus {
      border-color: #0de5ff;
      outline: 0;
      box-shadow: 0 0 0 .18rem rgba(13,229,255,.22);
      background-color: #21294a;
      color: #fff;
    }
    
    .tw-accent-border {
      border-color: #a855f7 !important;
      border-width: 2px;
    }
    
    .tw-panel {
      background: linear-gradient(128deg, rgba(13,229,255,.07) 0%, rgba(168,85,247,.08) 100%);
      border-radius: 1.1rem;
      box-shadow: 0 2px 30px 0 rgba(13,229,255,.08);
      border: 1px solid rgba(0, 229, 255, 0.2);
      backdrop-filter: blur(10px);
    }
    
    input[type="file"] {
      padding: 0.45rem 1rem;
      background: rgba(13, 229, 255, 0.06);
      color: #a855f7;
      border-width: 2px;
    }
    
    input[type="file"]::file-selector-button {
      background: #0de5ff;
      color: #21294a;
      border: none;
      padding: 0.42em 1.2em;
      border-radius: 0.55em;
      font-weight: 600;
      font-family: 'JetBrains Mono', monospace;
      transition: background 0.18s;
      margin-right: 1em;
      cursor: pointer;
    }
    
    input[type="file"]::file-selector-button:hover {
      background: #a855f7;
      color: #fff;
    }
    
    .form-text {
      color: rgba(148, 163, 184, 0.8) !important;
      font-size: 0.85rem;
      margin-top: 0.25rem;
    }
    
    textarea.form-control {
      resize: vertical;
      min-height: 80px;
    }
  </style>
</head>

<body class="bg-grid tw-antialiased">
  <form action="/admin/team/add" method="POST" enctype="multipart/form-data" class="p-4 tw-bg-gradient tw-panel tw-rounded-xl" style="width: 90%; max-width: 500px;">

    <h2 class="tw-header mb-4 text-center">Add Team Member</h2>
    
    <div class="mb-3">
      <label for="memberName" class="form-label">Name</label>
      <input type="text" class="form-control tw-accent-border tw-rounded-lg" id="memberName" name="memberName" placeholder="Eg: Aryan Kumar" required>
    </div>
    
    <div class="mb-3">
      <label for="memberRole" class="form-label">Role</label>
      <input type="text" class="form-control tw-accent-border tw-rounded-lg" id="memberRole" name="memberRole" placeholder="Eg: Lead, Designer, Developer" required>
    </div>
    
    <div class="mb-3">
      <label for="memberInfo" class="form-label">Short Info</label>
      <textarea class="form-control tw-accent-border tw-rounded-lg" id="memberInfo" name="memberInfo" rows="3" placeholder="Brief bio or introduction (1-2 sentences)" required></textarea>
    </div>
    
    <div class="mb-3">
      <label for="memberPhoto" class="form-label">Profile Photo</label>
      <input class="form-control tw-accent-border tw-rounded-lg" type="file" id="memberPhoto" name="memberPhoto" accept="image/*" required>
      <div class="form-text">Upload team member photo (JPG, PNG)</div>
    </div>
    
    <button type="submit" class="btn btn-brand mt-3 px-4 py-2 w-100 tw-rounded-lg tw-font-bold">Add Member</button>
  </form>

  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>